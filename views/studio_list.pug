extends layout

block content
  div(class="content")
    section(class="jumbotron text-center")
      div(class="container")
        hi(class="jumbotron-heading") #{title}
    div(class="album py-5 bg-light")
      div(class="container")
        div(class="row")
          for studio in allStudios
            div(class="col-md-4")
              div(class="card mb-4 box-shadow")
                img(src=studio.img_url class="card-img-top")
                div(class="card-body")
                  p(class="card-text") #{studio.name}
                  div(class="d-flex justify-content-between align-items-center")
                    a(class="btn btn-primary" href=studio.url) View details